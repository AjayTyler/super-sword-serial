<section class="tab sss-biography scrollable {{tab.cssClass}}" data-group="{{tab.group}}" data-tab="{{tab.id}}">
    {{formGroup systemFields.biography value=system.biography enriched=enrichedBiography
                toggled=true stacked=true rootId=rootId}}
</section>
